#name = "drfbc"
#author = "rebd"

coucou = "salut"

page accueil {
    image("https://tse4.mm.bing.net/th/id/OIP.ROkMXuS5vrwuhw3jgi_6BQHaLH?cb=12&pid=Api&ucfimg=1") {
        radius: 10px;
        center;
        height: 200px
    }
    title("Veuiller vous connecter") {
        var: super;
        center;
    }
    input() {
        var: in
        center;
    }
    button("Cliquer") {
        var: button
        width: 100%
        bottom;
        center;

        onClick {
            if (super.text == "Coucou") {
                super.text: "coucou est égal à salut"
            } else {
                super.hide;
            }
        }
    }
}
onKey ("Enter") {
    alert(in.value)
}